# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
# Patrón amplio a propósito (no sólo ".venv/" exacto): un venv con cualquier
# otro nombre (ej. ".venv-audit/", usado para pip-audit sin tocar el venv
# principal) se coló en un commit el 2026-07-13 porque el patrón anterior
# sólo cubría el nombre exacto. Ver REPOSITORY_CLEANUP.md.
.venv/
.venv*/
venv/
env/

# --- Entorno / secretos ---
.env
*.local

# --- Base de datos local ---
*.sqlite3
*.db

# --- Node / Frontend (futuro) ---
node_modules/
.next/
dist/
build/

# --- Editores / SO ---
.vscode/
.idea/
.DS_Store
