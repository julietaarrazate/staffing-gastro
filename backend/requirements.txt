# --- Core ---
# fastapi/starlette: pineados al mínimo que resuelve las 6 CVEs de starlette
# encontradas por pip-audit (2026-08-13, ver DUE_DILIGENCE_REPORT.md C4) —
# 0.115.6 permitía starlette<0.42.0 (0.41.3 instalada, vulnerable); hace
# falta cruzar a la serie 1.x de starlette para tener los fixes, lo que
# exige fastapi>=0.135.0 (0.130.0 todavía tope-a en starlette<1.0.0).
fastapi==0.141.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.3.0

# --- Base de datos ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Seguridad / Auth ---
pyjwt==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.32

# --- Notificaciones push (Web Push / VAPID, ver docs/reference/ACCESO_MODERNO.md) ---
pywebpush==2.3.0

# --- Tests ---
# pytest: pineado a 9.0.3+ (fix de PYSEC-2026-1845, ver DUE_DILIGENCE_REPORT.md C4).
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
aiosqlite==0.20.0
sentry-sdk[fastapi]==2.19.2
